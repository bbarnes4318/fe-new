const User = require('./User');
const Submission = require('./Submission');

// Define associations here if needed in the future
// e.g., User.hasMany(Submission);

module.exports = {
  User,
  Submission
};
